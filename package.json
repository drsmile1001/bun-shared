{
  "name": "@drsmile1001/lib",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "format": "bunx prettier . --write",
    "format:check": "bunx prettier . --check",
    "typecheck": "bun run --workspaces --if-present typecheck"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "prettier": "^3.6.2"
  }
}
